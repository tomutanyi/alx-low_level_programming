#include "main.h"

/**
*_isupper - A function that checks if a character is uppercase or not contained in the prototype header file
*@c: tested character by the program
*Return: O if not uppercase, if not return the number: 1..
*/

int _isupper(int c)
{
	if ((c >= 'A' && c <= 'Z'))

	return (1);

	return (0);

}
