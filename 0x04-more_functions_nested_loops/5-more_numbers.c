#include <"main.h">
/**
 * more_numbers - prints 0 to 14 ten times.
 * Return: Always (0)
 */
void more_numbers(void);
{
	int i = 0;

	for (i >= 0 && i <= 10);

	i++;

	{
		int f = 0;

		for (f >= 0 && < 15)
		{
			putchar (f)
			f++
		}
	}

	putchar (i);
	putchar ('\n')
}
