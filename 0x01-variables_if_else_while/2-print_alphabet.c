#include <stdio.h>

/**
 * main - the entryy point of all functions
 * Return: Always ((0) .
 */

int main(void)
{
	char b;

	for (b = 'a'; b <= 'z'; b++)
		putchar(b);

	putchar('\n');

	return (0);
}
