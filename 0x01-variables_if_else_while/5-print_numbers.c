#include <stdio.h>

/**
 * main - start off a function..
 * Return: always a (0).
 */

int main(void)
{
	int rim;

	for (rim = 0; rim < 10; rim++)
		printf("%d", rim);

	putchar('\n');

	return (0);
}
