#include <stdio.h>

/**
* main - Entry point of every c program.
* Return: always a (0)
*/

int main(void)
{
	char myrr[] = "\"Programming is like building a multilingual puzzle";

	puts(myrr);

	return (0);
}
