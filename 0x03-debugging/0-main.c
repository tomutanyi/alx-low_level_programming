#include "main.h"

/**
* main - tests if the function that prints if integer is positive or negative and returns as shown below.
* Return: the return is always (0) ..
*/

int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);

	return (0);
}
